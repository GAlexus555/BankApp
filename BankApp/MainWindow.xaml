<Window x:Class="BankApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BankApp"
        xmlns:vm="clr-namespace:BankApp.ViewModels"
        xmlns:views="clr-namespace:BankApp.Views"
        mc:Ignorable="d"
        Title="BankApp" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AccountViewModel}">
            <views:AccountView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TransactionViewModel}">
            <views:TransactionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ManagerViewModel}">
            <views:ManagerView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ClientTransactionViewModel}">
            <views:ClientTransactionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MyTransactionsViewModel}">
            <views:ClientTransactionView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
