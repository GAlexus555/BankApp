<UserControl x:Class="BankApp.Views.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankApp.Views"
             xmlns:vm="clr-namespace:BankApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:CardViewModel}">
            <local:CardView/>
        </DataTemplate>
    </UserControl.Resources>
    
    <DockPanel>
        <Grid Height="70"
              Background="LightGray"
              DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>

            <Label Content="{Binding Name}" FontSize="36" Grid.Column="0" RenderTransformOrigin="0.5,0.5"/>
            <Button Content="Überweisungen" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" FontSize="20" Grid.Column="1" Padding="10,0" Command="{Binding ShowTransactionsCommand}"/>
            <Label Content="Sparzinsen" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" FontSize="20" Grid.Column="2"/>

            <Button Name="Button_TurnOff" Content="⏻" Grid.Column="4" FontSize="36"></Button>
        </Grid>
        
        <!-- Cards container  -->

        <ItemsControl ItemsSource="{Binding Cards}" Grid.Row="1">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </DockPanel>
</UserControl>
