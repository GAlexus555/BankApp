<UserControl x:Class="BankApp.Views.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BankApp.Views"
             xmlns:vm="clr-namespace:BankApp.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:CardViewModel}">
            <local:CardView/>
        </DataTemplate>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <QuadraticEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <DockPanel Background="{StaticResource BackgroundBrush}">
        <!-- Header -->
        <Border DockPanel.Dock="Top" Height="64">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#1A56DB" Offset="0"/>
                    <GradientStop Color="#1E40AF" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <DockPanel Margin="16,0">
                <TextBlock DockPanel.Dock="Left" Text="💳" FontSize="20"
                           Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock DockPanel.Dock="Left" Text="{Binding Name}"
                           Foreground="White" FontSize="20" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0,0,20,0"/>
                <Button DockPanel.Dock="Left" Content="Überweisungen"
                        Style="{StaticResource HeaderButton}"
                        Command="{Binding ShowTransactionsCommand}"
                        FontSize="13"
                        VerticalAlignment="Center"/>
                <Button DockPanel.Dock="Right" Content="⏻"
                        Style="{StaticResource HeaderButton}"
                        Command="{Binding LogoutCommand}"
                        FontSize="18" Width="44" Height="44"
                        VerticalAlignment="Center" Margin="0,0,4,0"/>
            </DockPanel>
        </Border>

        <!-- Cards area -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,6">
            <ItemsControl ItemsSource="{Binding Cards}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
