<UserControl x:Class="BankApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.35">
                        <DoubleAnimation.EasingFunction>
                            <QuadraticEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0F172A" Offset="0"/>
                <GradientStop Color="#1E3A8A" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border x:Name="loginCard"
                Width="400" VerticalAlignment="Center"
                Background="White" CornerRadius="16" Padding="40,36">
            <Border.RenderTransform>
                <TranslateTransform/>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" ShadowDepth="8" Opacity="0.35" Color="#0F172A"/>
            </Border.Effect>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="loginCard"
                                             Storyboard.TargetProperty="RenderTransform.Y"
                                             From="22" To="0" Duration="0:0:0.4">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>

            <StackPanel>
                <TextBlock Text="💳" FontSize="38" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                <TextBlock Text="BankApp" FontSize="26" FontWeight="Bold" Foreground="#0F172A"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Melden Sie sich an, um fortzufahren"
                           FontSize="13" Foreground="#64748B"
                           HorizontalAlignment="Center" Margin="0,5,0,30"/>

                <TextBlock Text="E-Mail-Adresse" Style="{StaticResource FormLabel}"/>
                <TextBox Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,16"/>

                <TextBlock Text="Passwort" Style="{StaticResource FormLabel}"/>
                <PasswordBox x:Name="pbPassword"
                             PasswordChanged="PasswordBox_PasswordChanged"
                             Margin="0,0,0,26"/>

                <Button Content="Anmelden"
                        Command="{Binding LoginCommand}"
                        HorizontalAlignment="Stretch"
                        Padding="0,11"
                        FontSize="14"
                        IsDefault="True"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
