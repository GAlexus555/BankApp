<Window x:Class="BankApp.Views.AddClientDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Neuer Benutzer" Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <DockPanel>
        <!-- Header -->
        <Border DockPanel.Dock="Top" Padding="20,14">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#1A56DB" Offset="0"/>
                    <GradientStop Color="#1E40AF" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel>
                <TextBlock Text="Neuer Benutzer" Foreground="White" FontSize="17" FontWeight="Bold"/>
                <TextBlock Text="Konto anlegen" Foreground="#BFDBFE" FontSize="12" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- Form -->
        <StackPanel Margin="22,18,22,16">

            <!-- Name -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Vorname" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="tbFirstName" Margin="0,0,0,4"/>
                    <TextBlock x:Name="errFirstName" Text="Min. 3 Buchstaben, keine Ziffern."
                               Style="{StaticResource FieldError}"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Nachname" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="tbLastName" Margin="0,0,0,4"/>
                    <TextBlock x:Name="errLastName" Text="Min. 3 Buchstaben, keine Ziffern."
                               Style="{StaticResource FieldError}"/>
                </StackPanel>
            </Grid>

            <!-- Email -->
            <TextBlock Text="E-Mail-Adresse" Style="{StaticResource FormLabel}"/>
            <TextBox x:Name="tbEmail" Margin="0,0,0,4"/>
            <TextBlock x:Name="errEmail" Text="Bitte gültige E-Mail-Adresse eingeben."
                       Style="{StaticResource FieldError}" Margin="0,0,0,12"/>

            <!-- Password -->
            <TextBlock Text="Passwort" Style="{StaticResource FormLabel}"/>
            <PasswordBox x:Name="pbPassword" Margin="0,0,0,4"/>
            <TextBlock x:Name="errPassword" Text="Min. 8 Zeichen, 1 Großbuchstabe, 1 Ziffer."
                       Style="{StaticResource FieldError}" Margin="0,0,0,12"/>

            <!-- Phone -->
            <TextBlock Text="Telefonnummer" Style="{StaticResource FormLabel}"/>
            <TextBox x:Name="tbPhone" Margin="0,0,0,4"/>
            <TextBlock x:Name="errPhone" Text="7–15 Ziffern (Leerzeichen/Bindestriche erlaubt)."
                       Style="{StaticResource FieldError}" Margin="0,0,0,12"/>

            <!-- Address -->
            <TextBlock Text="Adresse" Style="{StaticResource FormLabel}"/>
            <TextBox x:Name="tbAddress" Margin="0,0,0,4"/>
            <TextBlock x:Name="errAddress" Text="Min. 5 Zeichen erforderlich."
                       Style="{StaticResource FieldError}" Margin="0,0,0,12"/>

            <!-- Birthdate -->
            <TextBlock Text="Geburtsdatum" Style="{StaticResource FormLabel}"/>
            <DatePicker x:Name="dpBirthdate" Margin="0,0,0,12"/>

            <!-- Role -->
            <TextBlock Text="Rolle" Style="{StaticResource FormLabel}"/>
            <ComboBox x:Name="cbRole" Margin="0,0,0,16" SelectedIndex="0">
                <ComboBoxItem Content="Kunde (client)"/>
                <ComboBoxItem Content="Manager"/>
            </ComboBox>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Abbrechen" Style="{StaticResource GhostButton}"
                        Width="110" Margin="0,0,10,0" Click="Cancel_Click"/>
                <Button Content="Erstellen" Width="110" Click="Create_Click"/>
            </StackPanel>
        </StackPanel>
    </DockPanel>
</Window>
