<UserControl x:Class="BankApp.Views.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="600">

    <DockPanel Background="White">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>

            <Label Content="🗺" Grid.Column="0"/>
            <Label Content="Kartennummer:" Grid.Column="1" VerticalAlignment="Top" Margin="10, 20, 10, 10"/>
            <Label Content="IBAN:" Grid.Column="1" VerticalAlignment="Bottom" Margin="10, 10, 10, 20"/>
            <Label Content="Betrag (placeholder)" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <Button Grid.Column="3" Content="➙" VerticalAlignment="Top" Margin="10, 20, 10, 10"/>
            <Button Grid.Column="3" Content="🖉" VerticalAlignment="Bottom" Margin="10, 10, 10, 20"/>


        </Grid>
    </DockPanel>

</UserControl>
