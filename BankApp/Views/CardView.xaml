<UserControl x:Class="BankApp.Views.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="180" d:DesignWidth="520"
             Background="Transparent">

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <QuadraticEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Border Style="{StaticResource CardBorder}" Margin="20,10" MaxWidth="500" HorizontalAlignment="Left">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1A56DB" Offset="0"/>
                <GradientStop Color="#1E40AF" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid Margin="22,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Logo + Status -->
            <DockPanel Grid.Row="0" Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Left" Text="💳 BankApp"
                           Foreground="White" FontSize="13" FontWeight="SemiBold" Opacity="0.9"
                           VerticalAlignment="Center"/>
                <TextBlock DockPanel.Dock="Right" Text="{Binding CardStatusText}"
                           Foreground="{Binding CardStatusBrush}"
                           FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"
                           VerticalAlignment="Center"/>
            </DockPanel>

            <!-- Row 1: Card number -->
            <TextBlock Grid.Row="1" Text="{Binding CardNr}"
                       Foreground="White" FontSize="18" FontFamily="Courier New"
                       FontWeight="Light" Margin="0,0,0,6"/>

            <!-- Row 2: IBAN -->
            <TextBlock Grid.Row="2" Text="{Binding IBAN}"
                       Foreground="#BFDBFE" FontSize="12" FontFamily="Courier New" Margin="0,0,0,14"/>

            <!-- Row 3: Balance + Expiry -->
            <Grid Grid.Row="3" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Cents}"
                           Foreground="White" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="GÜLTIG BIS" Foreground="#94A3B8" FontSize="9" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ExpireDate}" Foreground="White" FontSize="12"/>
                </StackPanel>
            </Grid>

            <!-- Row 4: Action button -->
            <Button Grid.Row="4" Content="→  Neue Überweisung"
                    Command="{Binding NewTransaction}"
                    Style="{StaticResource CardActionButton}"
                    HorizontalAlignment="Left"/>
        </Grid>
    </Border>
</UserControl>
