<UserControl x:Class="BankApp.Views.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BankApp.Views"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:BankApp.ViewModels"
             d:DesignHeight="100" d:DesignWidth="600"
             Background="Transparent">

    <Border Style="{StaticResource CardBorder}" 
            Background="LightCyan" 
            ClipToBounds="True"
            Margin="10">
        <Grid Background="LightCyan">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Label Content="Kartennummer:" 
                       Grid.Column="0" Grid.Row="0"
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Right"
                       Margin="0, 0, 10, 0"/>
                <Label Content="IBAN:" 
                       Grid.Column="0" Grid.Row="1"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Margin="0, 0, 10, 0"/>

                <Label Content="{Binding CardNr}" 
                       Grid.Column="1" 
                       Grid.Row="0"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="10, 0, 0, 0"/>

                <Label Content="{Binding IBAN}" 
                       Grid.Column="1" 
                       Grid.Row="1"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="10, 0, 0, 0"/>
            </Grid>

            <Label Content="🗺" 
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   FontSize="30"/>
            <Label Content="{Binding Cents}" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            
            <Button Grid.Column="3" Content="➙" VerticalAlignment="Top"
                    Command="{Binding NewTransaction}"/>
            <Button Grid.Column="3" Content="🖉" VerticalAlignment="Bottom"/>


        </Grid>
    </Border>
</UserControl>
