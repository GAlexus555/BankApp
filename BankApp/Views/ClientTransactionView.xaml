<UserControl x:Class="BankApp.Views.ClientTransactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Background="White">

        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="LightBlue" Padding="15,10">
            <DockPanel>
                <Button DockPanel.Dock="Left"
                        Content="← Zurück"
                        Command="{Binding BackCommand}"
                        Padding="8,4" Margin="0,0,15,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding ClientName}" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ClientEmail}" FontSize="12" Foreground="#555"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Transaction count -->
        <Border DockPanel.Dock="Top" Background="LightGray" Padding="15,6">
            <TextBlock FontSize="12" Foreground="#555">
                <Run Text="{Binding Transactions.Count, Mode=OneWay}"/>
                <Run Text="Transaktion(en)"/>
            </TextBlock>
        </Border>

        <!-- List -->
        <ListView ItemsSource="{Binding Transactions}" Margin="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Datum" Width="100"
                                    DisplayMemberBinding="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy}}"/>
                    <GridViewColumn Header="Von" Width="160"
                                    DisplayMemberBinding="{Binding IBANFrom}"/>
                    <GridViewColumn Header="An" Width="160"
                                    DisplayMemberBinding="{Binding IBANTo}"/>
                    <GridViewColumn Header="Betrag" Width="90">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding AmountEuros, StringFormat='€ {0:F2}'}"
                                           HorizontalAlignment="Right"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Beschreibung" Width="180"
                                    DisplayMemberBinding="{Binding Description}"/>
                    <GridViewColumn Header="Status" Width="80"
                                    DisplayMemberBinding="{Binding Status}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </DockPanel>
</UserControl>
